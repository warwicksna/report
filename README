This is the repository for our report

It has been organised so that each chapter is its own tex file, stored in the tex directory, and included in report.tex, similarly with images into the img directory